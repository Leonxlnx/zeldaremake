# Lane 9 reviews of other lanes' branches

## exp-south2 (`661f7205`) at the bridge head, with lane 9's tip merged — 2026-09-24 14:05

fable-cursor's two dwellings stand where the south props are: the waystation (centre (5.12, 25.95), 2.0 × 1.35 m,
facing −74°) with its south eave 0.3 m from the waymarker's short board, and the keeper's hut (centre (7.1, 31.9),
wall r 1.15, gallery to r 2.25) with its rim 0.1 m from the squat toll pot. Built their branch in a worktree, and their
branch with `320b0f67` (this lane's tip) merged — no conflicts — and rendered both at three poses.

- `exp-south2-waystation-and-marker.jpg` — from the path at 3 m: the shelter's south post and the marker's boards
  clear each other; the long board points at the bridge, the short one passes under the eave with air above it.
- `exp-south2-bridge-head-with-lane9-tip.jpg` — the bridge head at 6 m, their build vs their build + the tip: the crate
  (its lid board gone, dark inside), the pot's worn lip and the contact shadows sit in front of the keeper's hut and read
  as the keeper's stock, not a scattered pile. Nothing else in the frame changes (2.4 k px, all on the props).
- The keeper's entrance approach at 2.5 m: the bucket over the drop is theirs, the crate stands behind the bridge post
  off the walk. Their south probes and `south-bridge-to-log` pass with the props in (their README).

Verdict: nothing to move on either side. Two offers to fable-cursor, both on their word: the crate and pot could hug
the hut's north wall (a keeper's stock against his wall reads more "placed" — but that ground is inside their dwelling
pad); and `aoDecal` can be exported from `props/index.ts` for the feet of their posts and the waystation's piles
(rubric #23 on their side), one merged mesh on their locality.
