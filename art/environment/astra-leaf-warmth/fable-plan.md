Fable / Fable5: recovering the existing leaf-warmth 0.5 as a separate source slice on fresh 0963c09d. Exact helper/test from 0645b7d3; only the prior hook and program-key hunks in trees/materials.ts and trees/distant.ts. Current bark, near-floor profiles, geometry, wind, alpha and depth stay intact.

One attribution correction: the old helper already covers giantTreeNearCanopy (treeFragment with nearDetail='bark') and whiteTree. The existing seven-program CPU contract checks both; the near-base material remains excluded. No additional near-canopy shader or roof/moss tint is needed. I will run those three contracts, then matched native C/F on the current world and the prior fixed-baseline region metrics before sending the small source commit for import.

The thin bank shell and the sparse distant LOW crown both failed native review and remain local. Root, author and independent review agree: smooth opaque bank ovals persist under the added marks; the distant study makes overscaled leaf chips.

For the three bank groups you granted (24/25/26), the concrete replacement proposal recesses only their cores (radius x0.60) and uses layered existing near branches/leaves. CPU screen coverage of the unchanged near kit is only 26-37%; its present leaves sit inside the old core. Sizing two leaf layers gives about 7,100 leaves and at most 69,868 added leaf/wood triangles (<0.87% of F's 8.09M). Correction to the initial estimate: reusing the three near-part meshes adds zero mesh objects, but making these currently hero-cut parts persist adds up to 3 rendered draw calls. These are estimated ceilings, not measured candidate costs. Preserve original outer bounds, floor, corridors, RNG and no-shadow policy; selected-only persistent geometry, no global flat-swap flag. Group26 keeps its 4.75m floor, with its recessed solid floor above the new lower foliage. White-barks and plateau roof remain outside this lane.

Please acknowledge this precise update from retaining the core mass to recessing it. We are preparing one isolated CPU source prototype for review while you respond; no bank import or GPU capture yet. The warmth slice and its paired review go first.
