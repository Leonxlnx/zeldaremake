# Accepted source inventory: root83ebbc63 → canonical7a7a3502

Read-only comparison on 2026-09-22. **Six coherent source slices are absent from root83ebbc63: nine production files, three paired test files and one props README.** A fourteenth differing file is root's additional rocks B3 regression test; keep that root file. No source, assets, root checkout, other logs or ledger were changed by this inventory. `inventory.json` records exact Git blobs and protected equalities.

Character/model1873, atlas886c531c, fogc241593e, leaf warmth, the current three bank cores, moss, structures, terrain and hardscape are already identical. Do not re-import them. The inventory targets the two named immutable commits, not a moving HEAD.

## Source slices

| Slice | Exact accepted source commits, in order | Files to take | Evidence and remaining limits |
| --- | --- | --- | --- |
| Trees: storage and accepted LOD changes | `c938a862`, `79699a4f`, `58c39fc4`, `60408959` | `src/world/trees/index.ts` only | CPU arrays released after upload; range-checked normalized attributes; white-bark mid shadow pass removed; north-stand poles take far LOD after50m. These are four separable changes in one file, described below. |
| Rocks: dressing range and storage | `0d86abbb`, `20b72fdf`, `59c68f32`, `a1ed0427` | `src/world/rocks/index.ts` only | Clearing/backside dressing gets the accepted7–13m near-material fade. Pebble/all-rock storage compacts, then drops CPU arrays on upload. Existing B3 merged-count fixes are already in root; preserve them and the root-only test. |
| Props: upload release | `a7739410` | `src/world/props/index.ts`, `geometry.test.mjs`, `README.md` | Thirteen merged locality meshes release about7.9MB of CPU arrays after upload. Existing bounds are retained. The added CPU test invokes each upload callback on a probe and verifies precomputed bounds. |
| Grass: mid blades to26m | `f9c58007` | `src/world/vegetation/grass.ts`, `plants.test.mjs` | Accepted owner-requested fullness change; lodDistances6/16/16→6/26/26. Fable's current take0133 A reports8.83M/452draws, about+150K triangles from grass. Its final combined take is not sealed yet. |
| North-stand roof, final PR31 version | `57cea818` → `1af0d61a` → `4839a3c6` → `e7f39f61` → `ffff47b5`; accepted merge `bacdd46b` | `src/world/canopy/index.ts`, `roof.ts`, `roof.test.mjs` | Final source has grid z−96…−52, feather20m, stand cards1.35× with one more per clump. Own RNG/sector, original plaza buffers exact. Final PR body reports259clumps/1296cards/2592triangles, about+1draw/+3K native triangles, no errors; B/D/E SSIM +.0010/+.0013/+.0003, A/C/F unchanged. |
| Accepted owner audio | `c2c38485`, then `38823a67` | `src/audio/ambience.ts`, `footsteps.ts`, `index.ts` | Removes rapid loud rustle; softer wind/birds/steps and five analytic footstep surfaces. The second commit confines hollow-log classification to its actual traversed segment. This is separate audio behavior, not a character/animation patch. Fable reports an offline25s comparison; no new audio test or recorded mix appears in these two source commits. |

Use these as six named net source slices or the listed source-only hunks. Do not cherry-pick merge commits or whole branches, which also contain other agent logs, evidence and older imports. Taking the final pinned files is only appropriate while those destination files still match83; merge the actual hunks if a local lane has advanced.

## Tree details and evidence

- `79699a4f`: `releaseAfterUpload` on static geometries and every rebuilt pooled part, after bounds/byte measurement. [Canonical report](https://github.com/Leonxlnx/zeldaremake/blob/7a7a3502/art/environment/round51-poolmem/README.md): renderer RSS approximately−125MB on that machine, six-view output within4pixels/frame; existing lodPool tests10/10. This does not remove retained JS lobe records.
- `58c39fc4`: `compactAttributes` after root transforms and before byte accounting/upload hooks. Normals become Int8, in-range colours Uint8, in-range wind Uint16; position/UV/aRoot stay Float32. Out-of-range colour/wind keep Float32, so bank swap encodings and bright leaf warmth are not clamped. [Report](https://github.com/Leonxlnx/zeldaremake/blob/7a7a3502/art/environment/round51-vertexbytes/README.md): tree arrays318.1→228.1MB, canopy/base pools223.9→158.1 /33.1→26.0MB; six-view SSIM changes at most.0001, lodPool10/10/typecheck pass. These are author's existing results, not a rerun here.
- `c938a862`: **an accepted visual/performance trade**, not byte-neutral. White-bark mid LOD stops casting; columns retain it. [Report](https://github.com/Leonxlnx/zeldaremake/blob/7a7a3502/art/environment/round51-shadowlod/README.md): A approximately−60K triangles, C−240K, F−130K; C−.0006/D−.0002 SSIM. Fable5 additionally documented loss of some mid-distance crown/self/ground shade. Its original proof used the older haze; the new clarity can make that cost easier to see.
- `60408959`: only band-only poles north ofz−62 switch at50m×quality.distance instead of120m; the z−46 far-trunk row remains unchanged. [Report](https://github.com/Leonxlnx/zeldaremake/blob/7a7a3502/art/environment/round51-standlod/README.md): A/B/D/E approximately−50K triangles and+3draws, three arch poses pixel-identical; six views almost unchanged, D416changed pixels. The crossed-strip far trunk is still coarse geometry. Typecheck/build/lodPool10/10 reported pass.

For integration order, apply the storage and accepted triangle give-backs before the grass expansion and roof. Do not sum older measurements into a claimed current W38 headroom; their baselines, camera residency and haze differ.

## Conflict and preservation notes

1. **Keep root `src/world/rocks/tiers.test.mjs`.** Its extra real tile-builder→capture API→B3 test is absent from canonical. Replacing it from7a removes the `crossCheckAudit` import, test-only buildTiled exposure and all count/triangle/visibility/unsupported-declaration assertions. No upstream change in this file needs importing.
2. **Shared tree index needs a hunk merge once a candidate lands.** Current distant trial e19f477f and its support work touch distant submissions, instance data and pool scheduling. Preserve `STAND_FAR_LOD_M`/`isStandPole`, `compactAttributes`, release hooks and byte-accounting order when integrating that candidate. The giant-detail agent is already based on7a; its later patch must preserve those same hooks.
3. **The bank's generated data and leaf warmth source are exact already.** `giant.ts`, `nearCanopy.ts`, tree materials and the atlas painter do not differ. Existing persistent-parts audit and selected bank groups must survive the index merge. Compaction changes attribute storage/quantization only; do not restore old whole files to get it.
4. **CPU diagnostic compatibility:** after upload, tree/rock/prop attribute arrays become null. Production bounds/counts are computed before release, and character ground support reads hardscape meshes, which are unchanged. Diagnostics that raycast/read these arrays after upload must instead collect before upload or regenerate from source. Do not disable the upload hook to rescue a diagnostic.
5. **New roof and clearer fog were not measured together in its original pair.** [Merged PR31](https://github.com/Leonxlnx/zeldaremake/pull/31) contains the final six-view table; its canonical README still has placeholders for final/pose sections, and PR CI was cancelled at merge. Final local INBOX reports completed clearing/upward poses, but those are not a finished canonical report. Import the exact accepted source; retain this evidence limitation.
6. **No runtime character or asset replacement.** Entire `src/world/character/`, `public/models/`, atmosphere, structures, hardscape and terrain compare equal. Audio reads current player motion and layout for sounds; it does not change the physical support/footstep solver.
7. **7a's new admission message is not source.** Its INBOX adds a free/play-only, fixed-frames-identical condition for the future giant lane. That differs from the owner's all-camera clarity direction and Astra's announced same-camera-rule plan. Root and the giant-detail agent are handling that coordination; do not add a capture-mode exception during this source sync.

## Validation state and checks to reuse after a chosen import

Root83ebbc63 CI [35758081527](https://github.com/Leonxlnx/zeldaremake/actions/runs/35758081527) is **green**, including capture/score/anti-cheat. Canonical7a CI35760389622 and35760382681 were **pending** at this inventory. Fable's latest published tick233 has take0133 running and take0134 (fog, atlas, standLOD, roof, strap) queued. No sealed full-world measurement of that final combination was found.

After integration, reuse typecheck/build, `src/world/trees/lodPool.test.mjs`, `src/world/canopy/roof.test.mjs`, `src/world/props/geometry.test.mjs`, root's retained `src/world/rocks/tiers.test.mjs` and `src/world/vegetation/plants.test.mjs`. The audio slice has the existing `renderOffline` route if the parent chooses a listening check. This inventory ran no tests or captures and makes no fresh visual/performance claim.
