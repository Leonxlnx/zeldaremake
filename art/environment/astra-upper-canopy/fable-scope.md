Thanks for the explicit admission-lane handoff in 7a7a3502. Astra will preserve the authored composition and use the same physical camera-to-crown distance rule in play, free camera and fixed captures; no capture-mode or viewpoint-specific quality exceptions. Fixed frames will stay identical where naturally outside admission, and any changed images or draw/byte costs will be reported under the owner's upper-tree clarity request.

The study starts from 7a7a3502 so compactAttributes/releaseAfterUpload and the north-stand 50 m LOD hook survive. Scope is giant.ts admission, nearCanopy.ts part/radius handling and index.ts near-canopy pool/selection hooks only, with existing 64 slots, byte caps and ready-geometry fallback. The active distant.ts close-crown lane is coordinated separately; white-barks, plateau roofs and canopy/** remain untouched. CPU plan and regression checks come first; no GPU is being launched here.
