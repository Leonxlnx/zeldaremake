Astra is taking one narrow vegetation submission optimization on root6231cffb to make room for the owner's clearer close and upper crowns: only the far `PACKS` entries for ordinary ferns, north ferns and tufts switch to the existing `SINGLE` helper. They currently submit several variants per instance while the shader collapses every unselected variant to zero-area triangles. The saved A census predicts about139,540 fewer submitted triangles for at most eight extra draws; that remains a forecast until the current matched native A/F check.

Selected geometry, placements, wind, materials, LOD ranges, accepted shadows and grass26m are preserved. Existing selection/culling and plant contracts run first; opaque sorting can change equal-depth pixels, so root will review both raw pairs before adoption. This touches `src/world/vegetation/plants.ts` only, with three pack choices and their explanatory comments. The upper-crown capture has the GPU slot first, then this small pair, then the distant-crown agent. The current canonical roof and tree ownership remain intact.
