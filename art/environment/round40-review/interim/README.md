# Round 40 — interim comparisons (agents still running; NOT the seal)

Before = `0820f92` (take-0109). After = the round-40 branches at the commits below, mid-pass.
Final matched comparisons and the six-view take replace these when round 40 seals.

- `trees24-*.jpg` — r40/trees at `97b096a` (bole silhouettes + lantern-tree corridor `384aca7`,
  lantern limb twig forks + layered cupped laminae `934353f`, leaf-cluster rims on the cored lobes
  and distant crowns `27d7d93`, ragged moss on the column / denser darker limb laminae `97b096a`).
  Poses: A_stairs, plaza column at player height, stair landing looking back and looking up, the
  lantern limb from below, 3 m from the pods.
- `veg21-*.jpg` — r40/veg at `4c56f88` (clustered thin blades, verge transitions, near-camera
  frond detail `810b845`; frame A's circled bank face `5e39d5c`; verge band `981d2d0`; budget
  `4c56f88`). Poses: frame A + its right foreground (the owner's marked area), player height on
  the bank face, the audit's grass close-up and north-west base, a verge at 2 m.
- structures-25 (r40/struct at `2a6e249`: cap tufts, torn moss edge, plants, 1024 moss field) has
  no comparison sheet yet.

Honest read at this point: the marked right-foreground grass is visibly finer and denser; the
lantern limb carries more, darker, layered leaves but still reads as a smooth beam between them;
the far-crown "paddles" from the landing are barely changed by the rims so far.
