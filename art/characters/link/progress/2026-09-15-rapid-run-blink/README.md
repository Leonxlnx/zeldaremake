Run `npm run build`, then `node art/characters/link/capture_play_motion.mjs --smoke --rapid-run`. The capture helper defaults to this checkout; LINK_WORLD_ROOT can select another built checkout. Native D3D11 capture is intended for Windows. The24frame trace starts at simulation time30, runs6frames at60Hz, then releases input. Frame5 is fully closed; frame6 abruptly opens. This is a regression reproduction, not a passing continuity check.
