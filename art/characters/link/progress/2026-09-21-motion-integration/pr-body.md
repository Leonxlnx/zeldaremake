Link's run action retained 32 stale fractional arm keys between 57 intended poses, causing abrupt shoulder jumps when sampled more densely. Rebuild four shoulder/elbow rotation channels from the smooth, hash-pinned CC0-derived source while preserving the reviewed carriage, cadence, leg motion and other clips. The delivered GLB is `89df38f2`.

Stair support now reads outward timber crowns and steep upper shoulders; the existing planner and hip correction check the oriented sole interior before IK. Footprint measurement includes the complete ankle/toe hierarchy: the former ankle-only rule omitted210 low toe vertices and allowed measured309mm penetration. Stone-only sampling, analytic heights and layout are unchanged. The reviewed colour grade is baked into the asset and the runtime canvas pass removed.

Fable-3's prop publisher is imported unchanged from `e9a9fcdb` and connected before the walkable-platform collision exemption. Pots, crates and railings now block the player on terrain and decks. Ten raw-production CPU checks cover movement, airborne entry, route clearances and the west-deck corridor; the existing props tests also pass. Props retain the current wall policy at every height until landing on their tops is supported.

Validation: typecheck/build, portable continuity/preservation checks and negative controls, real-loader pixel/material parity, and physical ground regressions pass. Fresh actual-player testing covers1,320 stair frames and all537 low sole vertices including endpoint terrain:708,840 surface hits, no negative samples, page errors or reach clamps. Minimum clearance+1.187mmup/+1.267mmdown. A300-frame walk/run/idle video and five matched Blender pairs are provided. Native arm/body contacts improve1,452→1,316, peak99→22.

The earlier327-point evidence is explicitly retained as a historical subset. The expanded paired940-pose CPU result isolates the shared measurement fix and preserves flat-motion hashes exactly. A small regression runs directly against the production measurement function and fails the old toe-omitting implementation.

Remaining limits: extreme stair knees(169°/169°), visible thigh/tunic intersections, face/hand art and untested terrain approaches. Complete-foot bounds add23.4%/20.1% instrumented support calls over the earlier support correction; no FPS claim. Optional torso/landing/boot-tip/retiming trials remain held. Fable imported the previous character source/model pair and prop hook as084c2e67/c10bec08. CI was green atdef7c885; the complete-foot follow-up needs its own run.

[Images, video, reproduction and detailed findings](https://github.com/Leonxlnx/zeldaremake/blob/agent/astra-motion-sept21/art/characters/link/progress/2026-09-21-motion-integration/README.md)
