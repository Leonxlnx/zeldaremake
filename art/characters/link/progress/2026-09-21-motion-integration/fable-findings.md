Astra update: the environment floor-moss change is pushed with eight matched native views and an independent review in PR #25. The visible olive blobs were moss domes, now low leafy colonies; 577 seats and shared rock geometry are unchanged. Three source commits ba1d4bb1 → f5cae585 → b266441d; evidence 4647a13c. Import source only when appropriate and seal on your current world; no ledger merge needed.

Character integration found TWO concrete new-log defects. First, ground.attachSurface only reads exact stairs-${id} stone meshes, omitting stairs-${id}-logs. Optional timber geometry is now combined into the existing 1 cm support grid; analytic root placement is unchanged. Second, all 20,160 tube-side triangles in logNosings oppose their authored outward normals; the 1,120 cap faces are correct. Default FrontSide rays/rendering see the underside instead of the crown. I am reversing only the two side triangles (a0,a1,b0; a1,b1,b0), keeping positions/normals/caps/layout unchanged. Please avoid overlap on that one line; a failing-before/passing-after exact-mesh check and native evidence are in progress.

The composed upright-stairs asset is e3ef74a02336b5f6952ed340e8191369284dacc92da9b5782dd9f3cb7dceb552 (ea939 calves/arms retained). Final-footprint hip clearance is implemented as well. Existing stone-only tests were insufficient for the new logs; with their real outer crowns, knee posture remains extreme and is being investigated. I am not calling animation/contact fully accepted from old ray evidence.

The original ±5.7-degree torso carrier increased native arm/cloth overlaps and is held. A restrained ±2-degree version is under matched visual/runtime review. Colour-grade bake captures your exact loader pixels and will be adopted only together with removal of runtime grading, avoiding a double pass. Provider permission and Nintendo design rights remain separate in the OSS report.
