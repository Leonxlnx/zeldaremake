name: Astra lighting comparison
on:
  push:
    branches: ['agent/astra-link-movement']
    paths: ['gauntlet/scripts/capture-light-study.mjs', '.github/workflows/astra-lighting.yml']
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: astra-lighting
  cancel-in-progress: false
jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Render labelled baseline and lighting variants
        run: node gauntlet/scripts/capture-light-study.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: astra-lighting-${{ github.sha }}
          path: gauntlet/out/astra-lighting
          retention-days: 30
      - name: Save diagnostic comparisons on Astra's generated branch
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          CAPTURE_DIR="$GITHUB_WORKSPACE/gauntlet/out/astra-lighting"
          REVIEW_DIR="$(mktemp -d)"
          git -C "$REVIEW_DIR" init -b captures/astra-lighting
          git -C "$REVIEW_DIR" config user.name 'github-actions[bot]'
          git -C "$REVIEW_DIR" config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          gh auth setup-git
          git -C "$REVIEW_DIR" remote add origin "https://github.com/$GITHUB_REPOSITORY.git"
          if git -C "$REVIEW_DIR" ls-remote --exit-code --heads origin captures/astra-lighting; then
            git -C "$REVIEW_DIR" fetch origin captures/astra-lighting
            git -C "$REVIEW_DIR" reset --hard FETCH_HEAD
          fi
          cp "$CAPTURE_DIR"/*.jpg "$CAPTURE_DIR/lighting.json" "$REVIEW_DIR/"
          git -C "$REVIEW_DIR" add --all
          git -C "$REVIEW_DIR" commit -m "Astra diagnostic lighting comparison for $GITHUB_SHA [skip ci]"
          git -C "$REVIEW_DIR" push origin HEAD:captures/astra-lighting
