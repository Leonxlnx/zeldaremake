import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {execFileSync} from 'node:child_process';import {createHash} from 'node:crypto';import ts from 'typescript';import * as T from 'three';import * as U from 'three/examples/jsm/utils/BufferGeometryUtils.js';
const dir='gauntlet/tmp/lawn-pocket-612-review',base=execFileSync('git',['rev-parse','0f9426c'],{encoding:'utf8'}).trim(),candidate=execFileSync('git',['rev-parse','6128726'],{encoding:'utf8'}).trim();
const hash=a=>createHash('sha256').update(typeof a==='string'?a:Buffer.from(a.buffer,a.byteOffset,a.byteLength)).digest('hex');
function loader(ref,capture,overrides={},runtimeT=T){const cache=new Map();function load(f){f=path.posix.normalize(f);if(cache.has(f))return cache.get(f).exports;const m={exports:{}};cache.set(f,m);let s=overrides[f]??execFileSync('git',['show',ref+':'+f],{encoding:'utf8'});if(f==='src/world/materials/sprouts.ts')s=s.replace('        im.setMatrixAt(i, m.compose(p, q, sc));','        (globalThis as any).__reviewSpot?.(im, i, s);\n        im.setMatrixAt(i, m.compose(p, q, sc));');new Function('require','module','exports',ts.transpileModule(s,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText)(id=>id==='three'?runtimeT:id.endsWith('BufferGeometryUtils.js')?U:load(path.posix.join(path.posix.dirname(f),id+'.ts')),m,m.exports);if(f==='src/world/materials/sprouts.ts'){const original=m.exports.buildSproutMeshes;m.exports.buildSproutMeshes=(...args)=>{capture.spots=args[0].map(s=>({...s}));return capture.sprouts=original(...args);};}if(f==='src/world/hardscape/flagstones.ts'){const original=m.exports.placeFlagstones;m.exports.placeFlagstones=(...args)=>capture.paving=original(...args);}if(f==='src/world/hardscape/flowers.ts'){const original=m.exports.buildFlowerHeads;m.exports.buildFlowerHeads=(...args)=>{capture.heads=args[0].map(s=>({...s}));return capture.flowers=original(...args);};}return m.exports;}return n=>load('src/world/'+n+'.ts');}
async function build(ref,overrides={},runtimeT=T){const capture={instances:[]},read=loader(ref,capture,overrides,runtimeT),{WORLD}=read('config'),{LAYOUT}=read('layout'),tex=new Map(),audits={},ctx={config:WORLD,layout:LAYOUT,rng:read('util/prng').createRng(WORLD.seed),terrain:read('terrain/heightfield').createTerrain(),quality:{tier:'high',density:1,distance:1,shadows:true,pixelRatio:1},renderer:{capabilities:{getMaxAnisotropy:()=>8}},wind:read('wind/wind').createWind(),textures:{load:async(set,kind)=>{const key=set+'/'+kind;if(!tex.has(key)){const t=new T.DataTexture(new Uint8Array([127,127,255,255]),1,1);t.name=key;tex.set(key,t);}return tex.get(key);}},progress(){},audit(n,fn){audits[n]=fn;}};globalThis.__reviewSpot=(mesh,i,s)=>capture.instances.push({mesh,i,spot:{...s}});const system=await read('hardscape/index').create(ctx);system.group.updateMatrixWorld(true);for(const a of capture.instances){const m=new T.Matrix4(),c=new T.Color();a.mesh.getMatrixAt(a.i,m);a.mesh.getColorAt(a.i,c);a.matrix=m.toArray();a.color=c.toArray();a.variant=a.mesh.name+'/'+a.mesh.geometry.attributes.aSproutVariant.getX(a.i);}return{ref,read,ctx,capture,system,borrowedTextures:[...tex.values()],audit:audits.hardscape()};}

export {build,candidate,hash};
