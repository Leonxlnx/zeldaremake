# Squad brief — 10 parallel Opus chats (owner, 2026-09-23 06:50 UTC)

The owner is sending the block below to up to ten Opus 5.5 chats. Each chat gets the same block with
its own `LANE:` number. fable-cursor (this branch's Cursor agent) integrates: it reviews every draft
PR, merges what is better, republishes the walkable build, and keeps this file current.

**The owner's words (2026-09-23 06:50):** the trees do not populate (the middle distance is grey haze
where the reference has layered trees); the foliage is thick now, good; make the grass thicker on the
left side too; the background sound is too buzzy; the steps and his earlier changes; look at the real
game's references — how Link runs through and the path splits off into the forest; the people need
to be updated too; take initiative and work over Astra's areas; done in 24 hours so it looks amazing
and he can walk around a lot.

**The owner's marked screenshot (06:50):** play mode on the north path looking north from the plaza's
north end (Link ≈ (1.5, −14), camera at rest): red circle 1 = the column tree trunk left of the path, a
smooth pale cylinder in grey haze; red circle 2 = the empty grey middle distance over the path with a
jagged bright-blue streak (sky through a gap) where the reference shows tree crowns.

**What he should compare against:** `reference/frames-dense/review46/r_020.jpg`–`r_028.jpg` (his own
recording: Link runs from under the lantern bough along the stone path, the path bends past Saria's
mound and splits toward the house and into the woods, layered trees with readable round crowns at
every depth, soft mist, ferns and purple flowers at the verges), the 61 s demo
`reference/frames-dense/demo61/` (index in `reference/frames-dense/README.md`), his screenshots
`art/environment/owner-review-2026-09-19/ref-0[1-4]*.png` and his fix list in the README beside them.

**Live build he plays:** https://raw.githack.com/Leonxlnx/zeldaremake/play-head/index.html (the head
of `cursor/kokiri-world-phase1-f65e`, republished by fable-cursor after merges; the older
`monitor/play/` link only changes when a gauntlet take seals, ~6 h).

---

## The block to paste (change the LANE number, nothing else)

```
LANE: 1

You are joining a team of Opus chats improving a Three.js / Vite / TypeScript remake of Kokiri Forest
(Zelda: Ocarina of Time remake demo) in https://github.com/Leonxlnx/zeldaremake. The owner wants it
to look amazing and feel good to walk around in within 24 hours. Work autonomously and keep going
until the owner says stop; land something visible every 1–2 hours.

1. Setup: clone, `git checkout cursor/kokiri-world-phase1-f65e` (the integration branch), then
   `git checkout -b agent/squad<LANE>-<short-topic>`. `npm ci`, `npm run typecheck`, `npm run build`.
   Read AGENTS.md (architecture rules), docs/SQUAD_2026-09-23.md (this brief, the lane table, the
   owner's words and screenshot), docs/GOAL_MODE.md (earlier owner directives) and
   reference/frames-dense/README.md (the reference frames). Look at the reference frames yourself
   before changing anything, especially reference/frames-dense/review46/r_020.jpg … r_028.jpg.
2. Your lane is the row with your LANE number in docs/SQUAD_2026-09-23.md §Lanes. Stay in its files.
   If you must touch another lane's file, keep it minimal and say so in your PR.
3. See the problem first: render the owner's poses before you change anything
   (`node gauntlet/scripts/broll.mjs --dist dist --out /tmp/before --size 960x540 --shots <poses.json> --test --settle 6`,
   pose format in art/environment/owner-2026-09-23/shots.json; play mode through
   `node gauntlet/scripts/playtest.mjs --dist dist --out /tmp/play --only look,walk --shots`).
   Headless Chrome here renders with SwiftShader: a frame takes 20–60 s, so render few, well-chosen
   poses. If other agents share your machine, never run more than two Chrome jobs at once.
4. Make it better, then prove it: the same poses after, side by side (JPEG crops) in your PR, plus a
   play-mode check when walking, the camera or performance could change. An "after" that looks like
   its "before" is not a result. Judge at player height and 5–30 m, like the owner walks it.
5. Rules that do not bend: deterministic randomness only (src/world/util/prng.ts, no Math.random in
   world code); no Nintendo assets and never use reference frames as textures or scenery; CC0 or
   original textures, credited in public/textures/CREDITS.md; keep `npm run typecheck`, `npm run build`
   and the tests you touch green (`node --test <file>.test.mjs`); no force-push; never merge PRs.
   The owner's walk-around quality now beats the old fixed-frame gauntlet scores, but say in your
   PR if a change moves the six hero views (A–F) a lot.
6. Deliver: commit small, push often, open a DRAFT pull request into cursor/kokiri-world-phase1-f65e
   titled "squad<LANE>: <what it fixes>", and keep its description current (what changed, why,
   before/after images, how you tested, what is next). Do not edit .agents/INBOX.md (ten chats would
   conflict); your PR description is your report. fable-cursor merges good PRs quickly and
   republishes the build the owner plays; after a merge, branch again from the updated integration
   branch for your next item.
7. When your lane's list is done, take the next unclaimed item from docs/SQUAD_2026-09-23.md §Backlog
   (say "taking <item>" at the top of your PR description) or review another lane's open PR with
   before/after renders.
```

---

## Lanes

| LANE | area (files) | first items (the owner's words first) |
| --- | --- | --- |
| 1 | **Atmosphere and clarity** — `src/world/atmosphere/`, `src/world/lighting/`, `src/world/postfx/` | Substantially less grey washout: the middle distance must show trees, not haze (owner 09-22 and 09-23); mist soft and layered like review46 r_020–r_028; god rays subtle; the jagged bright-blue sky streak in the owner's 09-23 screenshot gone; exposure comfortable from ground to sky. The owner authorized working over Astra's former ownership here. |
| 2 | **Trees in the distance — "the trees do not populate"** — `src/world/trees/distant.ts`, `src/world/trees/index.ts` (LOD, pools), the canopy roof | Every direction you can walk shows layered trees with readable crowns and trunks at 15–80 m (review46); nothing pops in late or loads visibly after spawn ("make all the trees load in ASAP"); the far ring never reads as flat cards from below. fable-5 measured the owner's pose (INBOX 07:52): the north path is a trench of bare column trunks (the column "swap rule" in `trees/index.ts` turns pale white-barks in the hero frames' far wall into bare columns) with no leafy crowns at 10–40 m — his recording stacks round crowns there. Astra's PR #29 (layered foliage in three bank canopy cores, `57eea8c0`) conflicts with the head's memory-tier admission in `trees/index.ts`; its `nearCanopy.ts` layered-core records and `giant.ts` selection are the parts to port. |
| 3 | **Trees up close** — `src/world/trees/giant.ts`, `bole.ts`, the column trees, the white-barks | The column trunks are smooth pale cylinders (owner's red circle 1): bark relief, moss, root flare, colour that reads as wood; giants' roots and limbs at player height; trees read brown (bark), not green, as soon as you stand near them (owner 09-21). |
| 4 | **Grass and plants** — `src/world/vegetation/` | Thicker grass, the left side of the paths first (owner 09-23); lawns and verges as full as review46 (ferns, purple flowers, shrubs at the path edges); no bald or regular patches; keep the frame cost in check. Where the left side is held thin on purpose (tuned to the old fixed frames — the owner's walk now wins): the lawn band west of the spine is cut short (`grass.ts` `LAWN_BAND_CUT`, field.ts `lawnBand`), D's hollow loses blades and height (`D_HOLLOW_CUT` / `D_HOLLOW_HEIGHT`, field.ts `dHollow`), the north forest floor keeps 45 % (`NORTH_FLOOR_KEEP`), the trodden strip and D's shoulders are cut (`trod`, field.ts `dShoulder`). |
| 5 | **Sound** — `src/audio/` | The background bed is "too buzzy": a softer, natural forest (leaves in wind, birds at a distance, maybe water), no constant drone or insect buzz (the pod lanterns' hum was cut in `99af9adf`; start from there); footsteps match the surface (stone, grass, wood, leaves) at believable levels (owner 09-22). His 06:50 "the steps need to be like…" came right after "too buzzy" — most likely the footsteps: they were made surface-matched and softer on 09-22 (`footsteps.ts`, in the build he played) and still read as synthetic noise bands; make them sound like soft real steps (heel–toe, low body, little crinkle). `audio/index.ts renderOffline()` renders the mix to a WAV for before / after. |
| 6 | **Steps and paths** — `src/world/hardscape/`, path shapes in `src/world/layout.ts` / `src/world/terrain/` (coordinate with fable-cursor in your PR before moving layout) | The steps as in the owner's references (demo d_001–d_022, ref-03's right steps; the demo's walk north d_094 / d_104 shows LOG-RISERED steps — round timbers as the risers with earthy, mossy treads between — where ours are stone slabs with a log on each nose, `hardscape/stairs.ts` + `logNosings.ts`); the path splitting off into the forest as in review46 r_020–r_028 (toward Saria's house and into the woods), with walkable ground and matching edges. Walkability: `node gauntlet/scripts/playtest.mjs --only walk,climb` (nine routes, stuck points name what blocks). |
| 7 | **People (Kokiri)** — `src/world/character/npc.ts`, `kokiri.ts`, their placement | "The people need to be updated": faces, hair, clothes, proportions, idle motion and their fairies at the quality of Link; bring them back into the world (hidden since the owner's 09-20 request by `backgroundCast.visible = false` in `src/world/character/index.ts`; they must look upgraded when they come back — the owner judged them unchanged on 09-21) at the demo's spots (ref-01 girl with her fairy by the signpost; demo d_023–d_036, d_090–d_104 has kids on the path and the bank); the girl walks around (owner 09-19 item 7). Note `npc.ts` toggles a fairy light's `visible` (a light count change recompiles every lit program — keep light counts constant, use intensity). |
| 8 | **Link** — `src/world/character/glbLink.ts`, `link.ts`, `animation.ts`, `index.ts` (player) | How he runs through the forest in review46 r_020–r_028: stride, arm swing, lean, stairs; no sliding feet, no clipping. Astra's open PR #26 (`origin/agent/astra-motion-sept21`) has Link work not yet on the integration branch — `ec79e4ed` run curves + stair timber support, `5050496a` shorter boot tips, `be0e22f7` / `83ebbc63` running posture + shoulder strap, `14129340` / `06552ded` / `41de5a9d` heel, sole and hip contact (each carries a new `public/models/link/link-runtime.glb`; take the final GLB once, not every revision). Verify with `playtest.mjs --only climb,walk` (it reports the boots' contact). |
| 9 | **Houses, lanterns and props** — `src/world/structures/`, `src/world/props/` | Up close at player height: houses and huts finished (trim, openings, roofs, foundations), signs of use (pots, crates, baskets, benches), Saria's shelves no longer hollow (owner 09-21), lanterns lit restrained; the plaza as the demo shows it. |
| 10 | **Walkthrough QA and performance** — `gauntlet/scripts/` only (fixes go to the owning lane or fable-cursor as issues/PRs) | Walk every route on the live build in play mode (playtest.mjs walk routes and your own), compare against the references, post a ranked issue list with screenshots and exact positions in your PR; measure load time, frame pacing and hitches (shader compiles, LOD builds) on the head. |

## Places from the trailer (owner 2026-09-24 10:58: "build out the other places to visit from the video trailer that I can go explore past the tree")

From `reference/frames-dense/review46/` (contact sheet `review46-contact.jpg`). Each ships only at ≥ 170 / 200 on
`docs/RUBRIC_50_STRUCTURES.md`, walkable, every hero view ≤ 9.0 M / 700.

| place | trailer frames | where it attaches | who |
| --- | --- | --- | --- |
| waterfall ruins — mossy arches, terraces, waterfalls into pools | r_036–r_043 | a path west past the west house | `agent/fable-cursor-exp-ruins` (building) |
| Hyrule Field — open grassland, mountains | r_019 | out of the south log's far end | next for `agent/fable-cursor-exp-south2` after the log exit |
| the town square — half-timbered houses, red roofs, fountain | r_003–r_004, r_029–r_035 | beyond the field | open |
| the desert — dunes, cliffs, sunset | r_005, r_009–r_010 | beyond the field | open |
| the red-rock town | r_044–r_046 | beyond the desert | open |

## Backlog (take the next unclaimed item when your lane is done)

1. Saria's house interior: real objects on the shelves (owner 09-21: "hollow, nothing in them").
2. The north clearing and the ledge flight: something to do there (owner 09-19: "more to do after the steps").
3. The west house and the far hut at player height: walls read flat and dark.
4. The distant ring's floor cards seen straight up in the open north (a dark flat disc overhead).
5. Falling leaves, fireflies, butterflies: gentle life in the air, not spectacle.
6. The equipment bag screen against the demo (d_057–d_088).

## Integration (fable-cursor)

- Reviews every squad PR as it lands: renders the PR's poses on the head, merges when it is better,
  pushes, republishes https://raw.githack.com/Leonxlnx/zeldaremake/play-head/index.html and notes the
  merge in this file's log below.
- Owns the camera (`src/camera/`), `src/main.ts`, `src/world/index.ts`, `layout.ts` changes, the
  gauntlet takes and the evidence READMEs.

## Who has which lane (updated by fable-cursor)

| LANE | who | branch |
| --- | --- | --- |
| 1 | squad chat | `agent/squad1-haze-clarity` |
| 2 | squad chat | `agent/squad2-midcanopy` |
| 3 | squad chat | `agent/squad3-near-bark` |
| 4 | squad chat | `agent/squad4-verges-2026-09-23` |
| 5 | squad chat | `agent/squad5-forest-sound` |
| 6 | fable-2 | `agent/fable-2-earth-treads`, `agent/fable-2-ledge-logs` |
| 7 | fable-3 (from 12:55; the owner asked for the people) | — |
| 8 | Astra (from 17:00, PR #2 comment: Link's character + runtime, the #26 path; coordinates `trees/index.ts` with lane 2 before delivering) | Astra's PRs |
| 9 | fable-3 | `agent/fable-3-shelves-threshold` |
| 10 | fable-5 | `agent/fable-5-r54-review` |

**Integration rule:** fable-cursor merges pushed branches directly (a PR is welcome but not needed) after
tests and a render at the owner's poses, then republishes the play link. **Before each new push,
merge `origin/cursor/kokiri-world-phase1-f65e` into your branch** — the lanes share `trees/`,
`atmosphere/` and `vegetation/` neighbours and the head moves every hour.

## Review notes for the lanes (read before your next push)

- **Lane 2 — `squad2-lookup` merged at 05:20 and reverted (`7872ec0e`), fable-cursor 05:30.** At the pinned
  `u-open-up` pose the depth veil turns the crowns into pale beige flat shapes whose polygon edges read harder
  than before (mean 0.417 → 0.500, dark 36 → 10 %, 38.5 % of pixels changed): the owner's job 6 complaint was
  "flat cut-out cards with hard edges", and a paler card is still a card
  (`art/environment/owner-2026-09-23/pass5-fable-cursor/squad2-lookup-rejected.jpg`, before | after | changed).
  What would pass: the crowns' silhouettes soften or break up (alpha / lobe edges, depth-graded density) as
  they recede, with the tone moving toward the air only as much as the reference's r_025 band. Because the
  merge was reverted, re-merging the same commits does nothing — branch from the current head, `git revert
  7872ec0e` on your branch first, then add the fix on top.

- **Lanes 1 + 2 — from fable-5 (lane 10), 10:28, on `6664f739` at the owner's 06:50 pose against his
  r_024** (`.agents/reviews/fable-5-lane10-walk-2026-09-23.md` §6–§7, metrics `fable-5-lane10/bands.py`):
  the corridor is populated and warm now (the owner's "trees do not populate" is answered there), but
  **darker than before the squad**: mean lightness 0.300 → 0.230 (his 0.394), near-black 22 → 49 % of the
  band (his 12 %), the far-centre box 0.360 → 0.318 (his 0.474). **Lane 1:** the hue landed; the
  brightness went the wrong way — bring the far bands toward l 0.45–0.47 at his pose, hue kept, and less
  saturated (s 0.12 vs his 0.05). **Lane 2:** the 14–58 m crowns keep their local colour (green s 0.15 /
  l 0.29 vs his 0.05 / 0.42) — his distant crowns are pale warm silhouettes most of the way to the mist
  with light between them; and they roof the path's top band (0.335 → 0.232, his 0.418) where his
  recording keeps bright canopy gaps over the path. Give the mid crowns more of the veil with distance
  and keep the sky over the path open. fable-5 re-reads whatever lands within the hour at these poses.
- The six fixed views moved a lot (fable-5, `f56c5740` → `6664f739`: A −0.019, B −0.018, C −0.021 SSIM) —
  expected for the owner's direction; say in your commit when a change moves them further.

## Log

- 06:50 — brief written. play-head = `3b3a956a` (the owner's 09-23 passes 1–3: look-up camera, second
  staircase, lanterns, huts, veil, crowns from below, no shader recompile walking north, camera
  timing, stair lip / log grain).
- 07:15 — Astra's open PRs checked against the head: #25 (leafy moss patches), #27 (leaf atlases
  sRGB), #28 (leaf warmth) and #26's distant-crown clarity (`886c531c`) are already in; #29 / #30
  (bank canopy layers) and #26's Link chain are not (lane 8 row). `99af9adf` (lane 5's start): the pod
  lanterns' hum was the village's drone — now two soft sines under 420 Hz at a third of the level,
  only near a lantern. fable-cursor is decomposing the grey washout at the owner's pose (god rays,
  far softening, height fog shares) — lane 1 gets the numbers here before tuning. The new walk
  routes found single-frame camera jumps of ≈ 1.5 m (the follow camera running into the lantern limb
  / a house bough from behind) — eased in `8ab27c48`; the "snag" at (−11.4, 8.25) was the west fork's
  waymarker post on the route's straight line (a visible prop), not an invisible wall.
- 07:30 — INBOX note to fable-2/3/4/5 with the owner's priorities and lane fits. At the owner's pose
  the god rays' in-scatter is most of the grey veil (rays off: upper-left third 83.6 → 59.8 levels,
  upper centre 96.8 → 74.4); a between-the-beams / near-air comparison is rendering.
- 09:15 — the thinned-air variants are backed out (`7244aab6`; fable-5 lane 10 measured them against the
  owner's recording: the corridor's bright mist 2.0 → 0.9 %, his 22.6 % — lane 1: brighten and warm the
  mist instead, `reference/ANALYSIS_CLARITY.md` §5). The owner's pale cylinder = the far-trunk row, now
  dark boles (`75622db9`). D's hollow grass thicker in geometry, barely visible (lane 4). Merged fable-3's
  kindling basket (lane 9) and fable-2's 13 m near skin on the hero boulders. Lanterns measured
  (restrained pools, steady). play-head = `75622db9`. No squad PRs yet.
- 09:45 — five squad chats joined (lanes 1–5). Merged into the head: lane 1 `bca84c5a` + `f040df27`
  (the warm veil and light far air; the canopy gaps warm glare instead of blue — the owner circled the
  blue streak; he can ask for the blue back), lane 2 `dce3b33f` + `064a004b` (a mid-canopy layer for the
  14–58 m band), lane 3 `c0c0151f` … `06dd10c1` (column knees, near bark without lichen speckle,
  white-bark lenticel bands and scars), lane 4 `161425cb` … `f8d7b107` (walked-verge turf, violets,
  paddle leaves, fronds, clover; the west bank layered), lane 5 `4baa0633` … `0d9e0270` (the bed
  rebuilt, the lanterns as flame, heel–roll–toe footsteps). Tests 46 / 46. Batch 1 rendered at the
  owner's poses (violets and ferns along both edges of the north path, warmer far air); batch 2 (with
  the mid-canopy) rendering before the play link is republished.
- 09:55 — batch 2 rendered: at the owner's north pose round leafy crowns now frame the path (lane 2), the
  violets gather in masses (lane 4), the far air is warm and light (lane 1); toward Saria's and looking
  west the background fills with leafy trees at several depths (`art/environment/owner-2026-09-23/pass3/
  squad-north-three-states.jpg`, `squad-fork-west.jpg`). Sound: the scripted walk's hiss bands 1–16 kHz
  −4.5 / −3.7 / −6.4 / −11.9 dB, the drone −2.9 dB (`pass3/audio-mix-before-after-squad5.*`). Play link
  republished at `37c8471b`; it boots from githack in 83 s, every game resource loaded (the one blocked
  request is githack's own ad on its notice page).
- 10:05 — merged fable-4's understory (`c1988edc`: 44 round-crowned 4.5–9 m trees with brown trunks along
  the north path's verges, the clearing and the plaza's lawn edges — an import conflict with lane 2 in
  `trees/index.ts` resolved keeping both), lane 2's take 3, lane 4's verge shrubs, lane 1's evidence and
  haze-palette test. Tests 39 / 39. At the owner's north pose the path is now a corridor of real trees
  with leafy crowns over violet verges (`art/environment/owner-2026-09-23/pass3/owner-north-6664f739.png`,
  `plaza-fork-6664f739.png`). Play link `6664f739`: boots from githack in 70.5 s, every game resource
  loaded. fable-4 and lane 2 both populate the corridor — lane 2's cards at 14–58 m, fable-4's trees at
  3–11 m off the path: please look at each other's layers before adding more there.
- 11:20 — merged lanes 1–5's next pushes (lane 1: a third mist tier of tall thin curtains, the lit far wall
  over the closed roof; lane 2: the mid crowns' steep fade by distance; lane 3: columns' spreading feet,
  sun-side bark, leans, moss hue, the white-bark glow off; lane 4: the corridor's forest floor closed;
  lane 5: stair-riser steps, leaf litter, the wind relayered, birds +3 dB), fable-2's lane 6 first landing
  (earth treads between the log flight's timbers — the demo's log-risered steps) and fable-3's waymarker
  move. Tests 60 / 60. Play link `d49ecc9d` (boots in 58 s, 0 game failures). fable-5's 10:28 review is
  relayed above (§Review notes): lanes 1 + 2, the corridor is darker than before the squad.
- 13:00 — merged fable-3's trunk-avoid (an understory trunk grew through the plateau crate; props now keep
  off every published bole, with a test), fable-2's lane 6 ledge flight log-risered + the tread tint,
  fable-4's understory fork corner and arch verge. Lane 7 → fable-3 (the girl by the signpost first).
  Tests 38 / 38. Play link `5b11e161` (boots in 58 s, 0 game failures). No new squad-lane pushes since
  11:08; fable-2 flags A at 9.16 M triangles / 600 draws (over the 9.0 M cap) after the tree layers.
- 14:40 — merged fable-3's lane 7 first landing (`65606fe4`, `4b1759f9`: the girl by the signpost with a
  lock-lobed maroon bob, sheen, drape and fold ridges on the tunic, the head a third of her height; the
  cast visible again; the fairies' lights stay in the scene and dim by intensity) and fable-4's understory
  seated on the rendered ground (`a2d3097b`). Tests 24 / 24. Play link `52c07336` (boots in 58 s, 0 game
  failures); the girl stands at B's left with her fairy as in ref-01. fable-5 (13:43): A's 9.15 M
  triangles are lane 4's vegetation (+0.57 M), not the tree layers — lane 4, the turf's density or reach
  where A does not resolve it.
- 16:20 — merged the three perf branches fable-5 measured together (A 723 draws / 9.20 M → 698 / 8.87 M, both
  caps met with the kids visible): fable-2's flagstones stop casting (−180…−190 K on every view), fable-4's
  white-barks behind the camera cast from medium geometry (A −130 K, F −160 K), fable-3's kids cast only
  while their shadow reaches the view (A −25 draws) plus thinner brows (the frown at 5 m). Frames: B 0.18 %
  of pixels > 8 levels. Tests 33 / 33. Play link `7c2aa685` (boots in 61 s, 0 game failures). Lanes 1–5:
  no push since 11:08.
- 17:20 — Astra holds lane 8 (Link; PR #2 comment 17:00 — her local review build keeps the Kokiri hidden
  while she reviews the NPC source; the canonical branch shows them per the owner's 06:50 ask). Merged
  fable-2's earth under the log flight's timbers (`3cc8ca96` — fable-2's call to me: the owner's pose dark
  62.7 → 42.3 %, A −0.013 / F −0.008 SSIM; taken: the flight reads as the demo's earthen log steps and the
  scene was too dark against his recording), fable-4's finer understory leaves (two thirds the
  size, twice the count; A +17 K, D +49 K triangles, draws unchanged) and fable-3's lane 7 evidence.
  Budget on the merged head (fable-5's probe, capture mode, kids visible): A 692 draws / 8.88 M, D 556 /
  8.30 M, F 642 / 7.69 M — under both caps. Play link republished.
- 17:45 — the mid grove (lane 2) stands off the walked lines (`73fb7fb8`: crowns 3 m beyond the paving,
  boles ≥ 9 m from a centreline) after fable-5 §10 found mid trees 3–6 m from the owner's cameras (the
  north-hollow look-up filled with card quads, the west hut hidden, the path roofed); re-read at the same
  poses: the look-up 0.419 → 0.507 (pre-squad 0.550), the hut in view, the north path's top band
  0.275 → 0.300. Lane 1 still owns the upper house's dark (`b-upper-2`) and the far air's brightness.
- 19:50 — merged fable-4's understory walk clearance (`f5cf6c26`: 6.5 m off every walk line, 32 → 27
  stems, no other stem moves) and fable-3's lane 7 round (`e7a01c7e` the boy at Saria's door on the pass,
  `044fb636` the fairies read at 5 m, `e43ae92f` the kids turn their heads to Link within 5 m). Tests
  23 / 23. At the owner's north pose the verges keep their violets and ferns with the trees stepped back;
  at B the girl and the door boy stand with their fairies. Play link `b51f0954` (boots in 62.5 s, 0 game
  failures). Lanes 1–5: still no push since 11:08 — the owner can restart them with the block above.
- 20:45 — owner 20:08 (screenshot `art/environment/owner-2026-09-23/pass4/owner-2008-screenshot.png`): "no
  expansion to the environment past the stuff", "why don't the trees immediately spawn instead of needing me
  to get close" (red circle), "LOWER THE WHITE NOISE". Done: the noise layers cut (`b99166cd`: bed −6.9 dB rms,
  4–8 kHz −23 dB; full mix 4–8 kHz −16 dB, 8–16 kHz −13 dB); his red circle is the lantern tree's root on
  its smooth far base at 15 m (hero band 12 / 13.5 m on both tiers) — `39e63437` makes the large near-LOD
  tier the default below 8 GB / unreported memory and floors every near-base band at 40 / 44 m (the root
  now brown bark: `pass4/lantern-tree-root-before-after.jpg`). Expansion started on two branches:
  `agent/fable-cursor-exp-east` (a village lane on the east plateau after the main stairs: shop, a taller
  house, a small house, a lookout) and `agent/fable-cursor-exp-south` (the south exit: a path through the
  south giants to a rope bridge over a misty ravine and a lit tunnel mouth). Play link `39e63437`.
- 22:05 — merged fable-2's log-flight joint closed by geometry (`c37ffbd3` / `eb2c9cb5` / `78d18fe1`: the riser
  comes forward under each timber, the split tread laid as one earth tread, `LOG_SHADED_LIP` gone; stairs.test
  +204 lines — Astra's `s2-join-close` wedges answered: the close-up shows timber over packed earth, no dark
  joints), fable-3's kids skinned to their own joints (`814af6c9`: play at the stair foot 702 → 623 draws,
  8 px changed; B looks as before) and fable-4's understory evidence. fable-5 §18 measured `39e63437`: the
  bases are answered (all 23 resident, walk pacing unchanged), the crowns still built as he walked — so
  `94d96536` builds every pending part inside the pre-fetch radii on the first frame (1.5 s cap). Tests
  36 / 36 + 19 / 19. Play link `94d96536`.
- 23:05 — lanes 1 and 5 are back (pushes 22:08–22:32, branched from `6d145e90`, no PRs yet — merged on my own
  checks). Lane 1 `squad1-corridor-light` (heightfog: the corridor's 5–25 m band reads as air, the far veil a
  touch greyer): owner-0650-north mean 0.287 → 0.306, dark 45.9 → 40.8 %, top band 0.332 → 0.373 (his recording
  0.394 / 0.418), h-west-front dark 44.3 → 42.2 % (`pass4/corridor-air-squad1.jpg`). Lane 5
  `squad5-white-noise` (the wind bed a swell gated at a gust knee — the canopy's ungated wander was the floor;
  Link's landing sound; the bed filtered under the log tunnel): the bed's between-gust p10 −56.5 → −66.7 dBFS
  (before the 20:08 cut −47.9), gust p90 −43.3 → −42.5 (−37.2). Tests 14 / 14. Play link `5927d1eb`.
- 03:55 (Sep 24; the 23:10 tick arrived at 03:28) — the owner's 23:00 review is in `art/environment/owner-2026-09-23/
  pass5/` (fable-squad4: nine jobs, PROMPTS.md). Merged: squad4's stair camera (the aim and the orbit height
  eased against the staircase — climbing the main flight 23.7 → 4.95 m/s² vertical), lane 3's far-bark grain
  (the far programs' wood takes cords and furrows, the giants' far bark the sun side), lane 1's sky light on
  sky-lit surfaces, lane 4's verge near tier, fable-4's `nearCanopy.late` audit. Tests 64 / 64. **Accident:**
  fable-2's `0109a23b` tracked a worktree `node_modules` symlink; merging it replaced the real one with a link to
  itself — untracked (`cd55d004`), reinstalled, `.gitignore` `/node_modules` added. Job 5 (the lantern bough
  grey, pods unlit) does not reproduce on `6fcf5e64` in play mode (`play-shot.mjs`, first frame and after 2 s)
  — `pass5-fable-cursor/`. Play link `6fcf5e64`.
- 04:25 — **the south expansion is in** (`agent/fable-cursor-exp-south`, merged `31992fa4`): a 16.6 m path from
  the spine's south end round `plaza-south`'s foot, a ravine 11.4 m lip to lip and 9.2 m deep, a walkable 13.3 m
  rope-and-plank bridge with pods on its posts, and a hollow log's glowing mouth on the far bank
  (`art/environment/exp-south-2026-09-23/README.md`). On the merged head: tests 120 / 120; the walk plaza →
  bridge → log reached (21/21, nothing stuck), south probes 41 / 41 (the deck walks, 0.7–2.5 m off either side
  blocked), no page errors. Heroes: only C changes (2.25 % of pixels, +59 draws / +0.52 M); every camera under
  9.0 M / 700. Open for the camera lane: a follow camera snapped due south behind Link at the path's east edge
  (3.5, 27.6) sits inside `plaza-south`'s flare (a placed pose; the walked route never goes there). The east lane
  is finishing (its evidence was uncommitted). Play link `bd6487b1`.
- 04:50 — the owner's 23:00 jobs, second wave (merged on their evidence + my renders): squad4 `near-veil`
  (`02eca9aa`: the ambient base air gets a 4–16 m distance ramp — its height-only gate drew the band across tall
  trunks; `squad4-lantern-pods` reproduced the grey bough as that veil at 12 m), squad2 `treepop` (slot-rank
  hysteresis on the near crowns, the white-barks' first rung 20 → 28 m paid by the distant near gate 120 → 72 m),
  squad5 `music-rests` (the tune rests 16–30 s between passes, −3.2 dB on the bus; fairy glints; the wind leans
  upwind; the flutter gap capped at 2.2 s; the complaints as tests), fable-2 `stone-value` (the hero flight worn
  grey stone with a wandering lit nosing, its value up into the reference's band), fable-3 (thumbs, bent elbows,
  a standing idle with weight shift). Tests 134 / 134. A / owner-0650-north / B before → after:
  `pass5-fable-cursor/round14-stairs-veil-kids.jpg`. Play link `08b206cd`.
- 05:35 — merged squad5 `music-stable` (job 8: "the music was never shaking — the footsteps were pulsing over it
  at the step rate"; a render-capacity / voice-count diagnostic in `__ZR_AUDIO__.stats()`; the `index.ts` conflict
  with `music-rests` resolved by keeping both sides' fields) and squad4 `near-veil`'s second commit (the bough's
  wash decomposed term by term). squad2 `lookup` merged and **reverted** (see §Review notes). The final head is
  pixel-identical to `08b206cd` at `u-open-up` and the owner's 23:00 bough pose. Tests 45 / 45. Play link `5cbe6ac8`.
- 05:50 — PR #35 (lane 5's `music-stable`) carried one more commit, `eb29c813`: its own −3.2 dB music trim reverted
  (the rests give the forest its space; the trim only let the steps punch further over the tune). Merged; audio
  tests 23 / 23. Play link `5d0f4b9f` (boots, no game failures).
- 07:05 — owner 06:07 ("why is it white?" — most likely the log's flat white exit disc; "more structures along
  the path further down"; "50 rubrics for each item"): `docs/RUBRIC_50_STRUCTURES.md` written (50 checks, 0–4
  with evidence, ship ≥ 170 / 200); two new builders: `agent/fable-cursor-exp-north` (a grove with stilt / trunk
  houses, a rope walkway and a lookout past the second clearing) and `agent/fable-cursor-exp-south2` (the log's
  exit as a sunlit glade instead of the white disc; a tree house, a rim lookout and a rest spot on the south
  path). Merged: fable-3 south props + weathering by exposure, fable-2 ravine rock (outcrops in both walls,
  moss-capped floor boulders), squad5 south-exit audio (PR #39: the bridge its own surface, the log closes over
  you), squad4 midground (PR #37: the east verge's ground cover — the owner's asymmetry). Tests 128 / 128.
  `pass5-fable-cursor/round17-south-verge.jpg`. Play link `44025510`. squad2 `lookup` (PR #36) still waits on
  the §Review notes fix.
- 11:20 — owner 10:24 "make it ready, keep working and CHECK EVERYTHING". Merged PRs #40 (butterflies), #42 (the
  ravine opens the bed), #43 (the west verge's pale blobs were shot-D buds), #44 / #46 (audio evidence), #45 (the
  bud coil a flattened helix) and #41 (squad2 `softedge`: cards clustered, the veil at 0.85 — at `u-open-up` 5 %
  of pixels, no pale cards; the far canopy on the north path / D takes the haze, top band 0.372 → 0.392). Full
  check on `cc02a9cf`: 152 / 152 tests in 44 files; A–F and the owner poses ≤ 9.0 M / 700; **the south far-bank
  look-back 818 draws / 9.30 M — over** (exp-south2 owns it; lanes welcome); no collision-relevant source changed
  since `44025510`; the full walk run is queued. Play link `f04d9529`. W02 regressed on CI (26 treads since
  `f5015962`) — INBOX to fable-2.
- 17:45 (the box was paused ~12:35–17:30) — merged fable-2 `w02-treads` (the hero flight back to 20 × 0.27 × 0.54
  — A shows the worn grey flight, `pass5-fable-cursor/A-stairs-20-treads.jpg`), fable-3 (the girls' stitched belt
  and buckle; pots rubbed at the lip; darker crate slots), squad5 `live-capture` (the audio ran on rAF — the game
  was silent for its first six seconds), squad2 `crowntone` (PR #48: `u-open-up` pixel-identical, owner-0650-north
  1.7 %). Tests 129 / 129 + 25 / 25. Play link `ac137e7b` (boots, 113 s under load). Waiting, not merged:
  `squad5-merge-check` (it folds in exp-ruins before its builder reports), the four expansion branches (no final
  rubric reports yet), squad2 `roofhole`, fable-2 `pebble-far` / `cliff-scale`, fable-4 `eastroots`.
- 18:05 — merged lane 5's queue: #49 (the tune's never-ending sine gone, dynamics), #50 (the plateau lookout no
  longer sounds like lawn, with a guard test), #52 (a dropped-in music file no longer sets the mix by its mastering),
  #53 (a jump has a push-off sound), #56 (birds answer when the wind drops), #58 (step cadence corrected in the
  evidence renders), and fable-2 #57 (pebble tiles gated past 34 m — ~20 draws off the look-backs). Tests 171 / 171.
  Held: #54 squad2 `roofhole` (visual review). #55 squad5 `standing` came in anyway — lane 5's later branches
  were stacked on it — with its audio only (the exp-ruins zone is NOT in the head; checked). The four
  expansion builders resumed (all stopped mid-task again). Play link `eb8b727e` (world ready from the public link
  at 137 s under load).
- 22:20 — the north grove merged (`agent/fable-cursor-exp-north` @ 330db5d0: rubric 174 / 170 / 171 / 170, walk
  28/28, probes 64/64, every view ≤ 700 / 9.0 M); tests 178 / 178; play link `b9993008` (the owner recorded his
  video on it). 23:45 — after the recording: PRs #54 (the canopy closes over the open north, the south exit and
  the grove shelf), #60, #61 (the flight's tops shaded per vertex), #62, #63 (the game's level gain-staged +12 dB
  to desktop level), #64, #65 (the understory's medium LOD keeps every lamina), #66, #67 (birds on perches), #68
  (the rope walk sounds like a bridge). Tests 183 / 183. Play link `8c60f0bd` (boots, 118 s).
- 00:20 (Sep 25) — merged #70 (squad5 `indoors`: walking inside a hut sounds like a room) and #69 (fable-2 notes).
  Audio tests 47 / 47. Play link `2f6c8ae2` (boots, 109 s).
- 01:20 — merged #73 (squad5 `room-answers`: Link's boots sound indoors inside a hut), #71 / #72 (notes, lane 2's
  head re-measure). Audio + character tests 55 / 55. Play link `c58123e2` (boots, 106 s).
- 02:15 — merged #74 (fable-4: white-bark medium crowns keep their laminae), #75 (fable-2: rock `scale` + a
  sandstone palette for the trailer's cliffs), #77 (squad2: a test that fails if a roof grid is deleted), #76 / #78
  (notes, lane 5's floor re-survey). World tests 111 / 111. Play link `463c2cde` (boots, 125 s).
- 02:55 — merged #79 (squad5: the stereo pans with where Link faces), #81 (fable-4: the north stand's 50 m far-LOD
  rule retired), #82 (fable-3: kids' belts and cuffs, seated flaps, a 30 m cull), #80 / #83 / #84 (notes, the
  "trees look fake" measurement, lane 5's 50-check sound rubric). Tests 191 / 191. Play link `ab4719bc` (boots, 69 s).
- 04:45 — the VM was reset (`/tmp` and `node_modules` gone; reinstalled with `npm ci`). Merged #85 / #88 (squad5:
  sound occlusion — a bird behind the west house loses its top), #86 (squad2: the trees' wood materials named),
  #87 (fable-3: three distinct girl looks across the cast). Tests 196 / 196. Play link `313fec4a` (boots, 72 s).
- 05:25 — merged #89 (squad2: backlog 3, the west house's walls, measured), #90 / #91 / #92 (notes and lane 5's
  evidence). Tests 196 / 196. Play link `cfeefd11` (boots, 70 s).
- 06:10 — merged #93 (fable-3: the cast's variety, second pass — the boy's look), #47 / #94 / #95 (fable-4's round-54
  notes, fable-2 notes, squad2's far-hut measurement). Tests 196 / 196. Play link `41939301` (boots, 66 s).
- 07:10 — merged #96 (squad5: the forest bed no longer loops the same nine seconds), #99 (fable-3: the seated
  girl lifts her head off her knees), #100 (squad5: a run sounds like a run, not a louder walk), #97 / #98 (notes,
  backlog 2 measured). Tests 199 / 199. Play link `3556b945` (boots, 66 s).
- 07:55 — merged #101 (fable-4: the giants' near-canopy lobes and limbs draw as one batch — fewer draw calls),
  #103 (squad5: footsteps no longer drop when the frame rate dips), #102 (notes). Tests 202 / 202. Play link
  `e7fd8ea2` (boots, 72 s).
- 08:45 — merged #104 (fable-4: the near-canopy batch's heap cost stated as measured), #105 / #106 / #107 (notes;
  squad2 flags play mode over the triangle gate at the flight's foot). Tree tests 25 / 25. Play link `26625539`.
- 09:40 — merged #110 (squad2: backlog 4 — the dark flat mass overhead in the open north fixed), #111 (squad5: the
  audio graph no longer keeps every finished event's nodes), #108 (fable-2 notes + a small fix). Held #109 (fable-3
  marks it "evidence in progress — hold"). Tests 206 / 206. Play link `8e20e128` (boots, 66 s).
- 11:25 — merged #109 (fable-3: the wandering girl stops and greets Link), #115 (fable-2: hardscape shading arrays
  drop their CPU copies after upload — less memory), #117 (fable-4: the canopy batch trims its reserve after
  evictions), #112 / #113 / #114 / #116 / #118 (verification notes). Tests 206 / 206. Play link `a2bd75ed` (boots).
- 12:10 — merged #119 (fable-2: a `cpuArrays` audit — CPU bytes kept by rocks / hardscape), #121 (squad5: a
  diagnostic that cost a frame removed), #120 (notes). Tests 206 / 206. Play link `8d7eb41c` (boots, 75 s).
- 12:55 — merged #122 (fable-3: the north grove's second person — a boy at the stilt house's veranda), #125
  (squad5: a step sounds exactly when a boot lands), #126 / #127 (rocks' load-time and trees' CPU-array audits),
  #123 / #124 (notes; squad2: "trees load in ASAP" measured on screen). Tests 206 / 206. Play link `11cd6ba6`.
- 13:55 — merged #130 (fable-3: the standing kids greet Link — the ledge girl and others turn to him), #128 / #129
  / #131 (notes; squad2 flags the two climbing look-backs 25 % over the triangle gate, fable-4 answered). Tests
  206 / 206. Play link `ce688584` (boots, 68 s).
- 14:32 — merged #132 (fable-2: rock position welds grouped once — the rocks' load build 9.3 → 6.6 s), #134
  (squad5: the canopy's sound closes over Link where he stands, not 4 m back), #133 (squad2: notes — a third of each
  frame's triangles is the sun's depth pass; the giants' wood is not in it). Held #135 (fable-3 marks it "evidence
  in progress — hold"). Tests 209 / 209. Play link `ca75ccbf` (boots, 112 s on the VM's software GL).
- 15:15 — merged #135 (fable-3: the standing kids and the wanderer give one 0.8 s nod as they turn to greet Link),
  #136 (fable-2 notes: the hardscape build profiled — flat, left alone), #137 (squad2: the sun's depth pass split —
  two thirds solid world, trees 19 %, grass barely casts). Tests 209 / 209. Play link `2722c196` (boots, 66 s).
  The three trailer-place builders had been idle ~17 h with their areas unshipped (`exp-east` 36 commits ahead,
  `exp-south2` 35, `exp-ruins` 34; each conflicts with the head in 9–11 shared files). Relaunched as background
  sub-agents with a finish-to-ship brief (merge the head, fix the README punch lists — east: F 9.05 M and the
  lane look-backs over 700 / 9.0 M, deck steps as wood, the lookout fence's open ends; south2: the far-bank LOD
  A/B to ≤ 700 / 9.0 M, the exit glade, the end-to-end walk; ruins: the budget, walkability, an evidence README —
  then the 50-check rubric, re-merge the head, push): east `bc-5c2029e8`, south2 `bc-4d92d7f8`, ruins
  `bc-dc37fc66`; worktrees `/tmp/wt-exp-{east,south2,ruins}`, preview ports 4311 / 4312 / 4313, Chrome through
  `/tmp/treebase23/capslot.sh` (recreated — `/tmp` had been cleared).
- 16:16 — merged #139 + #143 (squad5: a bird is a place — its pan follows Link while he turns under it and moves
  as he walks past, instead of freezing where he faced when the call was booked 4 s ahead; +2 tests), #140
  (fable-3: the boy at Saria's door takes the stands' idle in play and greets with the turn and the nod; capture
  poses unchanged), #138 / #141 / #142 (notes: fable-4's depth pass by tree family; squad2 — the giants' trunks
  draw 1.36 M triangles at any distance, 15 % of hero A, no LOD rung; fable-2 iteration 127). Tests 211 / 211.
  GitHub auth dropped for a few minutes (gh 401, one push and the play-head push refused; retried by hand). Play
  link `0455f06c` (the page serves the new assets). The builders are pushing: east at the lane probes, south2
  at its evidence poses, ruins at the ruins' footstep surfaces.
- 19:10 — **pending integration, Astra's ask (PR #2 19:05): PR #59** (`codex/walk-arms-sept24` @ `ae894d5d`, base
  `main`): running hands/arms + the walk's vertical shiver. Integrate once its CI passes, the paired files TOGETHER:
  `src/world/character/animation.ts`, `src/world/character/glbLink.ts`, `public/models/link/link-runtime.glb`
  (sha256 aa0520e0…), `public/models/link/SOURCE.md`. The canonical branch carries Astra's GLB Link from PR #26 @
  `be0e22f7` (`3e34a3cd`) but not main's 59 later root commits — apply PR #59's diff against main to those four
  files, check it applies (else rebase on main's versions), run the gait / character tests and a play-mode run.
