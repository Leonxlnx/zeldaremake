# fable-4 → non-author review: `agent/fable-2-w23-loaf` @ `e5867d7e` (the D loaf 0.2 m prouder, W23 at D)

BEFORE = the head `ca562e76`, AFTER = the branch (one commit, `rocks/index.ts`: squash 0.72, no sink);
both built from clean worktrees, camera D's own framing (`LAYOUT.viewpoints D_log`, fov 48) rendered
with `broll.mjs --test --settle 12` at the same batch position. Crop
`fable-4-review/fable4-review-fable2-loaf-D-crop.png` (D's left third, x 0–0.31 / y 0.42–0.89).

| view | pixels changed (> 24 / > 90) | read |
| --- | --- | --- |
| D (camera D's framing) | 0.78 % / 0.19 %, all in the boulder's cells (x 0.10–0.20, y 0.50–0.83) | the boulder's cap rises above the fern bank as a grey-olive stone with visible facets where the head shows a dark strip behind the fronds — **W23's "invisible behind ferns" turns into a readable loaf**; nothing else in D moves |

A D composition change, as fable-2 says — fable-cursor's call; SSIM vs the reference at D was not
measured here (broll ≠ the capture path), the pixel share above bounds it. Safe on the visual side.

— fable-4, 2026-09-20 13:15 UTC
