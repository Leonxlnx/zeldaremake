# Astra layered fringe evidence

Original12-lock candidate for Link, baseline9a317b873eae4a036e0ad179027201fdfdb217c1. No baseline source tree is duplicated here: `loader.mjs` reads the pinned Git object and overlays retained candidate files. This package can be copied intact to `.agents/reviews/astra-layered-fringe`.

`candidate/src` is the final fringe source and its narrow Link append on that baseline. `composed/src` adds the reviewed lower-jaw helper and visible-face factory hook; its hair buffers remain exact. `fringe.patch` changes only fringe construction and the interleave append; preserve any newer unrelated Link hooks when applying it.

The final candidate adds four fine interleaves and reshapes eight thick bundles as shallow unequal sweeps. Hair gains1352vertices/2688triangles in the same draw.72 other meshes/286 arrays,14 protected hair charts, all original53208hair indices, eight root centres and98 temple end position/normal vertices remain exact. See `handoff.md` for detailed method, clearances, limitations and first failures.

Run from repository root with its Node dependencies installed and baseline Git object available:

```
node .agents/reviews/astra-layered-fringe/replay.mjs
```

Use this package's current path if it has not yet been copied. Default replay verifies the byte manifest, pinned baseline comparisons, normal-only equivalence and composed jaw/fringe clearance, then verifies the retained files were unchanged. It writes only to `gauntlet/tmp/astra-layered-fringe-evidence-replay`. It does not render images or publish anything.

The composed check proves final hair position/normal/UV/index byte equality to the candidate, all8 ocular/brow mesh arrays and local transforms unchanged, and a full changed-fringe/skull gap of .216621890mm. The lower jaw affects only the visible face carrier. The unchanged eye geometry allows the retained9blink clearance evidence to carry over; the default replay does not repeat it.

`reports` retains the original physical/contact results. `snapshot02` preserves exact shape before39 selective geometric normal repairs. Final replay proves every position/UV/index byte unchanged, with minimum repaired incident-face dot .05353445. Two protected old temple fan tips retain four slightly negative inherited corner dots (worst−.025286). `failures` preserves the original self-crossing root blend, failed simple normal averaging, and caught temporary-normal alias bug.

Optional independent physical replays are `contact-review.mjs`, `cap-contact-review.mjs`, `attachment-review.mjs`, and `self-crossing-review.mjs`. Their output also goes to scratch. Every nonzero-area indexed triangle is retained. The12roots have real finite foundation intersections (shortest measured segment .2729mm); all self-crossing, seated cap and9discreteblink checks passed. This is finite static/pose evidence, not an all-time collision proof or visual acceptance.

Three planned layer pairs remain separated (2/5,5/9,0/6); every lock still attaches to foundation. Actual face/profile captures must assess the silhouette, brow coverage and those visual gaps before acceptance. No world, shader, material, movement, screenshot, rubric or other-agent source changed in this candidate.
