# fable-5 — round-52 reviews (`agent/fable-5-r52-review`)

Continues `fable-5-r51-branches.md` after `23704533` merged it. Same method.

## A. Iteration 40 (14:26–15:05 UTC) — the merged head `f6890f4a` (log nosings in), W02 pre-read; take-0129 pending

Tick 207 merged fable-2's log nosings (`93fdff4a`, the version I measured in r51 §D), the W23 vegetation
follow-up (the hero fern clump authored at the frame's spots) and slots64, and launched take-0129
(13:59); it has not sealed by 15:00. Character-on render of A on the merged head against take-0128's
frame and the reference (sheet `fable-5-r52/fable-5-r52-w02-preread.jpg`): every step of the hero flight
carries a dark rounded bark timber along its edge with moss on the crown and a stake at the log ends every
second step; the cut-slab faces are under the crowns. It is the frame's log-risered flight in kind.
**W02 turns to pass on take-0129 on my read**; the criterion's letter ("individually cut, weathered
stone") is the rubric text RUBRIC_PROPOSALS asks to change — the reference it names is timber-nosed.
F's ≈ −0.010 (r51 §D) is the owner-approved cost, to be named at the seal.

Still open from §9 after the logs: the pitch (ours gentler), the banks rising beside the flight, the
top dissolving into a haze gap (V17).

## B. Iteration 42 (16:24–16:55 UTC) — fable-4 `agent/fable-4-northstand` @ `7fec320f`: the stand beyond the arch (V2 / opus #01, round-50 #3)

A dense stand of the 26 m poles beyond the north clearing — two flanks (|x| 12–34, z −82…−64) and a back
stand behind the ledge terrace (z −90…−81), own streams, placed after the radial pool so the existing far
layer stays byte-identical. Head `f6890f4a` + branch; tsc + build green.

| pose | head → stand | SSIM vs reference |
| --- | --- | --- |
| A_stairs, C_lookback | **pixel-identical** | Δ 0 |
| D_log | 0.04 % | −0.0002 |
| `x-arch-tunnel-n` (the `d_121` pose) | 2.65 % — the window's far layer thickens with tall trunks | window l 0.294 → 0.284 (frame 0.326) |
| `x-northpath-n` (into the clearing) | 17.7 % — a wall of tall trunks in haze behind the clearing where the head had a few | — |

**IMPROVED — the first half of V2's structural gap: the far layer beyond the arch is vertical structure
in haze, not a plain with cones.** From the north path the clearing now ends in trunks the way the
frame's window does. What the window still lacks against `d_121`: the trunks are **dark** where the
frame's are lit pale (window l 0.284 vs 0.326 — the stand darkens it a touch), there are no glowing dots
or lantern points, and the ground plane (path, ledge flight, signpost) is still in the window where the
frame dissolves it into light. Merge; the lights and the lit trunk value are the next two halves
(atmosphere / distant material), the ground plane is terrain north's.
Sheet `fable-5-r52/fable-5-r52-f4-northstand.jpg`.
