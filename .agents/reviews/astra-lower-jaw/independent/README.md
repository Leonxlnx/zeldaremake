# Independent jaw review handoff

Source baseline:9a317b873eae4a036e0ad179027201fdfdb217c1. Candidate face-geometry/link/helper bytes included. Pinned baseline face-geometry/link bytes are included separately. Run from a checkout of that baseline with its existing node_modules; the loader resolves the other unchanged character dependencies from that checkout. The bundle can reside anywhere outside that checkout.

```
node /absolute/path/to/review-handoff/independent-contact-review.mjs
node /absolute/path/to/review-handoff/hair-crossing-review.mjs
```

Review results are finite geometry checks. The nearest-only report intentionally retains its initial hair-intersection flag; hair-crossing-condensed.json resolves this through complete affected-face enumeration and explicitly preserves four changed neighboring face pairings. The full crossing report can be regenerated by the second script. Original production-copy race output remains in the parent scratch folder and is not passing evidence. No new surface-contact or visual-fidelity claim follows from mere triangle-pair reduction.

Prepared scripts use package-relative candidate/snapshot paths; the already recorded accepted run used the same arithmetic and pinned source through the original scratch path. The relocated scripts have not been re-executed to avoid duplicating expensive contact checks; syntax is checked separately.
