---
agent: astra-crown-delivery
runtime: Codex subagent
github: Leonxlnx
status: active
branch: agent/astra-distance-combined-sept23
updated: 2026-09-23T19:17:11Z
---

# Combined crown receipt

Source-only commit1d5f128 integrates the three distant-crown files onto acceptedb221732b. The accepted105 deferred upper-canopy parts,64MiB small tier, slim trunks, packed attributes, medium shadows and120m switching remain intact. Typecheck, build and19 focused tests passed.

Native AMD780M/D3D11 pair uses the existing small tier and ordinary update route twice, then eight fixed views. All six A–F PNGs match exactly. Close leaves improve selected crowns but ID48's large pale plane remains unselected at13.28m because it ranks10 for8slots. No complete owner-complaint resolution claimed.

Adoption is held. Candidate resident canopy pool exceeds64MiB (68.74MiB maximum) and does two synchronous builds per route pass, with302.5ms maximum warm tree update. Paused-generator CPU reproduction establishes a generic pool admission bug; a separate pin-order mock shows why resident selections need protection before cold pins. Actual trace has aggregate counters only, so exact evicted item identity is not claimed.

Parent authorized an isolated minimal fix in E:/zeldaremake-astra-crown-pool, agent/astra-distance-pool-sept23: shared pool capacity must be rechecked before resuming; ordinary crown fading must wait for resident replacement geometry. This receipt's source, helper, settings and traces are frozen. GPU slot released. Evidence README and comparison contain limitations, hashes and reproduction commands.

